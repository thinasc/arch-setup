# Action Key - Set prefix to Ctrl-t instead of Ctrl-b
unbind C-b
set -g prefix C-t
bind-key C-t send-prefix

set -g repeat-time 0

# Shortcuts to Projects
bind-key -r B run-shell "tmux-sessionizer $HOME/projects/timearc-ccms-backend"
