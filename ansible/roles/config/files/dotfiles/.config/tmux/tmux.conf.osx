# Action Key - Set prefix to Ctrl-Space instead of Ctrl-b
unbind C-b
set -g prefix C-Space
bind-key C-Space send-prefix

set -g repeat-time 0

# Shortcuts to Projects
bind-key -r K run-shell "tmux-sessionizer $HOME/projects/klelyaevents.com"
